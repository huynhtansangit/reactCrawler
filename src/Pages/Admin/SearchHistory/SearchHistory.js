import React from 'react';
import Page from '../../../components/Page'

const SearchHistory = () => {
    
    return (
        <Page title="Search">
            <h1>SearchHistory</h1>
        </Page>
    );
};

export default SearchHistory;
